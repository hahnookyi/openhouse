# openhouse

「에어비앤비 트렌드 연구 밋업 - 웰컴 커뮤니티」 행사 안내 **단일 페이지**.
GitHub Pages 로 배포된다(`.nojekyll` 있음, Vercel 아님).

## 가장 중요한 것: 이 파일은 손으로 고치지 않는다

`index.html` 은 396줄이지만 **3.40MB**(3,569,822 bytes) 다. 382줄 한 줄이 3,530,710자짜리
base64 이미지 번들(`<script type="__bundler/manifest">`)이고, 나머지는 그것을 푸는
언패커 코드다. **사람이 쓴 소스가 아니라 생성된 결과물이다.**

- 이 파일을 직접 편집하지 마라. 문구 하나를 고치려고 3.5MB 줄을 건드리면 안 된다
- 내용을 바꿔야 하면 **원본에서 다시 생성해 파일 전체를 교체**한다
- 이 저장소에는 원본이 없다. 수정 요청이 오면 **원본이 어디 있는지 먼저 묻는다**
- grep 이나 부분 읽기로 탐색할 때는 반드시 긴 줄을 걸러서 본다:
  `awk 'length($0) < 300' index.html | grep ...`

## 구성

```
index.html    번들된 단일 페이지 (396줄 / 3.40MB)
.nojekyll     GitHub Pages Jekyll 처리 비활성화
og-image.png  / share.png     공유 카드 이미지
wmh-logo.png / ai-living-stay-white.png
```

빌드 도구도 package.json 도 없다. 도입하지 마라.

## 안전하게 고칠 수 있는 것

`index.html` 을 다시 생성하지 않고도 되는 작업:

- **OG/공유 이미지 교체** - `og-image.png`, `share.png` 파일만 바꾼다
- **로고 교체** - `wmh-logo.png`
- 위 파일들은 통째로 교체하면 되므로 번들과 무관하다

`<title>`, `<meta>` 같은 문서 앞부분(1~34줄)은 번들 바깥이라 기술적으로는 수정 가능하지만,
다음번 재생성 때 덮어써진다. 고쳤다면 원본에도 반영해야 한다고 반드시 알린다.

## 글쓰기

- 행사 안내 문구는 한국어. 담백하게 쓰고 과장된 마케팅 톤을 쓰지 않는다.
- em dash 와 en dash 는 쓰지 않는다. 하이픈(-)만 쓴다.
